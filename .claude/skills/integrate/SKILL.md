---
name: integrate
description: Run `make ci` with a sensible branch name and commit message, and keep fixing issues until it passes.
---

Run `make ci BRANCH=<branch-name> MSG="<commit message>"`, picking a sensible branch name and commit message from the current changes. Keep fixing whatever it complains about and rerunning until it passes.
